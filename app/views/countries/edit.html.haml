%h1
  = t('country.edit.title')

= form_for(@country, :url => {:country_id => params[:country_id], :action => 'update'}) do |f|
  = render(:partial => 'form', :locals => {:f => f})
  .actions
    = f.submit I18n.t('submit')


