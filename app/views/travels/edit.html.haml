%h1
  = t('travel.edit.title')

= form_for(@travel, :url => {:travel_id => params[:travel_id], :action => 'update'}) do |f|
  = render(:partial => 'form', :locals => {:f => f})
  .actions
    = f.submit I18n.t('update')
