%h1 
  =t('travel.new')

= form_for(@travel, :url => {:travel_id => params[:travel_id], :action => 'create'}) do |f|
  = render(:partial => 'form', :locals => {:f => f})
  .actions
    = f.submit I18n.t('submit')

