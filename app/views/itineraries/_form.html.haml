
- if @itinerary.errors.any?
  #errorExplanation
    %h2= t('activerecord.errors.messages.itinerary.invalid_record')
    %ul
    - @itinerary.errors.full_messages.each do |msg|
      %li= msg

= f.fields_for :itinerary_contents do |builder| 
  = render 'itinerary_content_fields', :f => builder

-#
  .field
  - f.fields_for :itinearies do |builder| 
    = render 'itinerary_fields', :f => builder 
