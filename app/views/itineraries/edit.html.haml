%h1
  = t('itinerary.edit.title')

- form_for(@itinerary, :url => {:itinerary_id => params[:itinerary_id], :action => 'update'}, :html => {:multipart => true}) do |f|
  = render(:partial => 'form', :locals => {:f => f})
  .actions
    = f.submit I18n.t('update')
