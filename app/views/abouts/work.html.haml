= breadcrumb :about_work

#sidebar-first
  = render 'sidebar_first'

#content
  %h1.page-title
    = I18n.t('about.work')

  .content
    - about_infinity.each do |about|
      = paragraph(about.work_with_us)
    = link_to I18n.t('applicant.breadcrumb'), new_applicant_path, :class => 'button new'


