#content
  %h1.page-title
    = t('press_release.edit') + ' ' + @press_release.title
  
  = render 'form'
  
  == #{ link_to t('show'), @press_release, :class => 'btn cancel' } #{ link_to t('back'), press_releases_posts_path, :class => 'btn cancel' }
