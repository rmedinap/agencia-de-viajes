%h1 
  New Complex


= form_for(@offer, :url => {:offer_id => params[:offer_id], :action => 'create'}, :html => {:multipart => true}) do |f|
  = render(:partial => 'form', :locals => {:f => f})
  .actions
    = f.submit I18n.t('submit')

